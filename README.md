This Linux Bash script is intended to help learn Linux Bash commands.

Note : tldr and man commands should be installed in Linux before the script run.

To run the script change user permissions to allow execute:  
```bash
    
    chmod u+x LinuxExam.sh 
    ./LinuxExam.sh
```

